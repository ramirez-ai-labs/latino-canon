# Dependabot disabled 2026-09-21
# Reason: Auto-PRs were creating lockfile sync cycles and breaking builds
# Re-enable with stricter rules when ready (security updates only, no major versions)
# See: https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuring-dependabot-version-updates#enabling-dependabot-version-updates

version: 2
updates: []
  # npm updates disabled - manual review only
  # github-actions updates disabled - manual review only
